{
    "cmake.debugConfig": {
            "environment": [
                {
                    // add the directory where our target was built to the PATHs
                    // it gets resolved by CMake Tools:
                    "name": "PATH",
                    "value": "${env:PATH}:D:/workspace/dev/vcpkg/installed/x64-windows/bin:D:/workspace/dev/vcpkg/installed/x64-windows/debug/bin"
                },
            ],
            // attach
            // "type": "cppvsdbg",
            // "request": "attach",
            // "name": "Attach to local process",
            // "processId": "${command:pickProcess}",
    }
}